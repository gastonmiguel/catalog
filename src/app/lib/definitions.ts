export type Company = {
    id: string;
    name: string;
    slug: string;
};

export type Product = {
    id: string,
    name: string,
    slug: string,
    imageSrc: string,
    description: string,
    price: string,
    sku: string
}